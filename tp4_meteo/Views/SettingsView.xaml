<Window x:Class="tp4_meteo.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        
        xmlns:p="clr-namespace:tp4_meteo.Properties"
        
        Title="{x:Static p:Resources.MenuConfig}" Height="250" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <StackPanel Margin="20">

        <TextBlock Text="{x:Static p:Resources.LabelApiKey}" FontWeight="SemiBold"/>
        <TextBox Text="{Binding ApiKey, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Padding="5"/>

        <TextBlock Text="{x:Static p:Resources.LabelLang}" FontWeight="SemiBold"/>

        <ComboBox SelectedValue="{Binding Language}" 
                  SelectedValuePath="Tag"
                  Margin="0,5,0,20" Padding="5">
            <ComboBoxItem Content="Français" Tag="fr" />
            <ComboBoxItem Content="English" Tag="en" />
        </ComboBox>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{x:Static p:Resources.BtnClose}" Command="{Binding CloseCommand}" Width="80" IsCancel="True"/>
            <Button Content="{x:Static p:Resources.BtnSave}" Command="{Binding SaveCommand}" Width="100" Margin="10,0,0,0" IsDefault="True"/>
        </StackPanel>
    </StackPanel>
</Window>