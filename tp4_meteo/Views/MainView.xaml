<Window x:Class="tp4_meteo.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:tp4_meteo.Views"
        xmlns:vm="clr-namespace:tp4_meteo.ViewModels"
        
        xmlns:p="clr-namespace:tp4_meteo.Properties"
        
        mc:Ignorable="d"
        Title="{x:Static p:Resources.AppTitle}" 
        Height="520" Width="880"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="{x:Static p:Resources.MenuConfig}" Command="{Binding OpenSettingsCommand}"/>
        </Menu>

        <local:WeatherView Grid.Row="1" DataContext="{Binding WeatherVM}" />
    </Grid>
</Window>