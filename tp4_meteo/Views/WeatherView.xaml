<UserControl x:Class="tp4_meteo.Views.WeatherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:c="clr-namespace:tp4_meteo.Views.Converters"
             
             xmlns:p="clr-namespace:tp4_meteo.Properties"
             
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">

    <UserControl.Resources>
        <c:BoolToVisibilityConverter x:Key="BoolToVisConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{x:Static p:Resources.LabelRegions}" 
                       Grid.Row="0" FontSize="16" Margin="0,0,0,5"/>

            <ListBox Grid.Row="1" 
                     ItemsSource="{Binding Regions}" 
                     SelectedItem="{Binding SelectedRegion}" 
                     BorderThickness="1" BorderBrush="#CCCCCC">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <DockPanel LastChildFill="False">
                            <TextBlock Text="{Binding Nom}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </DockPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border Grid.Row="2" BorderBrush="#CCCCCC" BorderThickness="1" Margin="0,10,0,0" Padding="10" Background="#F9F9F9">
                <StackPanel>
                    <TextBlock Text="{x:Static p:Resources.LabelNom}" Margin="0,0,0,2"/>
                    <TextBox Text="{Binding NewNom, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{x:Static p:Resources.LabelLat}" FontSize="10"/>
                            <TextBox Text="{Binding NewLat, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="{x:Static p:Resources.LabelLon}" FontSize="10"/>
                            <TextBox Text="{Binding NewLon, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </Grid>

                    <Button Content="{x:Static p:Resources.BtnAdd}" Command="{Binding AddRegionCommand}" Margin="0,10,0,0" Height="25"/>
                    <Button Content="{x:Static p:Resources.BtnDelete}" Command="{Binding DeleteRegionCommand}" Margin="0,5,0,0" Height="25"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Column="2">

            <Grid Visibility="{Binding SelectedRegion, Converter={StaticResource BoolToVisConverter}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="{Binding SelectedRegion.Nom}" FontSize="22" FontWeight="Bold" Margin="5,0,0,0"/>
                </StackPanel>

                <ListView Grid.Row="1" ItemsSource="{Binding Previsions}" BorderThickness="0">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Date" DisplayMemberBinding="{Binding Date}" Width="110"/>

                            <GridViewColumn Width="60">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding Weather.IconUrl}" Width="35" Height="35"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn Header="Max" DisplayMemberBinding="{Binding HighTemp, StringFormat={}{0}°C}" Width="60"/>
                            <GridViewColumn Header="Min" DisplayMemberBinding="{Binding LowTemp, StringFormat={}{0}°C}" Width="60"/>
                        </GridView>
                    </ListView.View>
                </ListView>
                

            </Grid>
        </Grid>
    </Grid>
</UserControl>